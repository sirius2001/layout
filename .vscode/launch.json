{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/server/main.go",
            "args": ["-conf", "${workspaceFolder}/config/config.json"], // 相对路径更加清晰
            "env": {
            },
        }
    ]
}
